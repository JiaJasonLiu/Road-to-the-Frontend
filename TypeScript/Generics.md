Generics are essential because they make the function **polymorphic** (meaning it can work with multiple types). 
- They act as temporary, typed variables that are determined at the call site. 
- Without them, the function cannot capture the specific literal type of the key.