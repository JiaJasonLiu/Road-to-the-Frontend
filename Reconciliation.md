Is the process of React to figure out how to efficiently update the [[Document Object Model (DOM)]] when a change occurs in the UI.
- used for useEffect, useCallbacks and when the dependencies changes

Done:
- By updating the DOM to match the virtual DOM